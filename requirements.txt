# 为什么需要 requirements.txt：
# 锁定依赖清单，保证任何机器（本机 / CI / Docker）执行
# `pip install -r requirements.txt` 后得到一致的环境。
# 引入原则：每个模块用到才加，不提前堆。

pytest>=8.0
playwright>=1.40
requests>=2.31  # M4：API 层会话客户端（2.34.2 实测通过）
pymysql>=1.1  # M6：DB 校验层（直查本地 MySQL 验证业务真实落库）
allure-pytest>=2.13  # M8：Allure 报告（2.16.0 实测通过；HTML 生成需另装 allure CLI）
