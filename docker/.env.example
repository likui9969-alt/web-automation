# compose 口令默认值（本地学习环境专用，非生产敏感）
# 用法：需要改口令时复制本文件为 .env 并修改（.env 已被 .gitignore 屏蔽，不入库）
# 保留默认值即开箱即用——注意与 cli_install_config.yaml 中应用账号口令保持一致，
# 否则安装器创建的应用账号口令与运行环境不一致。
MYSQL_ROOT_PASSWORD=root_password_local
MYSQL_PASSWORD=ohrm_password_local