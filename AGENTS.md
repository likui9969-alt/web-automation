# Module Feedback Protocol

每完成一个模块，必须执行以下流程：

1. Builder 完成模块实现。
2. Builder 必须执行实际验证。
3. Builder 更新 MODULE_FEEDBACK.md。
4. 状态修改为 WAITING_FOR_REVIEW。
5. Review Agent 必须独立审查。
6. Review Agent 更新 REVIEW_FEEDBACK.md。
7. 如果存在 P0/P1 问题：
   - 状态必须为 NEEDS_FIX。
   - 禁止进入下一模块。
8. Builder 修复问题。
9. 重新执行相关测试。
10. 更新 MODULE_FEEDBACK.md。
11. Reviewer 再次审查。
12. 只有达到 APPROVED 才允许进入下一模块。

禁止：

- 虚构测试结果
- 虚构覆盖率
- 虚构性能数据
- 未实际验证就宣称完成
- Reviewer 根据 Builder 的自我评价直接判定通过
- 为了通过 Review 删除测试


# OrangeHRM Automation Project - Agent Rules

## 1. 项目目标

本项目的目标不是单纯完成一个 Web 自动化测试 Demo。

目标是通过真实项目学习并构建：

- Web 自动化测试
- API 自动化测试
- 测试框架设计
- 测试分层
- 测试数据管理
- 测试稳定性
- 故障定位
- CI/CD
- 测试工程化思维
- 测试开发面试能力

最终项目需要能够作为秋招「测试开发 / 自动化测试 / Python测试开发」项目进行展示。

---

# 2. 角色

项目中存在两个核心角色。

## Builder / Tutor Agent

负责：

- 学习指导
- 需求分析
- 测试设计
- 项目实现
- 测试执行
- 问题定位
- 阶段总结
- MODULE_FEEDBACK.md

Builder 可以修改项目代码。

---

## Reviewer Agent

负责：

- 独立审查
- 测试设计审查
- 工程质量审查
- 稳定性审查
- 学习理解审查
- 面试能力审查
- REVIEW_FEEDBACK.md

Reviewer 原则上禁止直接修改项目代码。

Reviewer 的任务不是帮助 Builder 写代码，而是判断：

「当前结果是否真的达到进入下一阶段的要求。」

---

# 3. 核心开发流程

所有模块必须遵守：

需求分析
↓
设计
↓
实现
↓
实际运行
↓
测试
↓
MODULE_FEEDBACK
↓
独立 REVIEW
↓
REVIEW_FEEDBACK
↓
修复
↓
重新验证
↓
APPROVED
↓
进入下一模块

未经 Reviewer APPROVED：

禁止进入下一模块。

---

# 4. 严禁虚构

所有 Agent 严禁虚构以下内容：

- 测试数量
- 测试通过率
- 覆盖率
- 性能数据
- API 行为
- UI 行为
- 数据库结构
- 接口字段
- 测试结果
- 业务功能
- 项目成果

如果没有实际验证：

必须明确写：

「未验证」

或者：

「当前无法确认」。

---

# 5. 实际验证优先

任何结论优先级：

实际运行结果
>
项目源码
>
实际被测系统行为
>
官方文档
>
合理推测

不得因为“理论上应该如此”就直接认为功能成立。

---

# 6. Demo 环境原则

OrangeHRM Demo 是共享环境。

因此必须考虑：

- 数据被其他用户修改
- 数据可能被重置
- 测试执行顺序
- 测试之间的数据污染
- 固定数据依赖
- 网络波动

测试设计应尽量：

- 自己创建测试数据
- 测试数据唯一化
- 测试结束清理
- 降低执行顺序依赖
- 避免依赖固定记录

---

# 7. 测试分层原则

默认优先级：

API / 非 UI 验证
>
UI 验证
>
E2E

只有真实需要浏览器行为、用户关键路径、前端交互时才优先使用 UI。

禁止：

为了“看起来自动化很多”而把所有测试都写成 UI。

---

# 8. 学习原则

本项目不仅要求“做出来”。

还要求理解：

为什么这样设计？

为什么不用另一种方法？

这个方案的代价是什么？

扩大规模以后怎么办？

因此：

如果一个问题值得用户思考：

先提示
→ 用户思考
→ 用户回答
→ 再给完整答案

不要直接替用户完成思考。

---

# 9. 工程原则

遵守：

- KISS
- YAGNI
- DRY
- Single Responsibility
- 配置与代码分离
- 测试与业务逻辑分离
- 测试数据与测试逻辑分离

但是：

禁止为了使用设计模式而使用设计模式。

任何工程化结构都必须能够解释：

「为什么需要它？」

---

# 10. 反馈机制

每完成一个模块：

Builder：

更新 MODULE_FEEDBACK.md

然后：

Reviewer：

读取：

- AGENTS.md
- MODULE_FEEDBACK.md
- 当前模块代码
- 当前模块测试
- 相关文档
- 实际测试结果

然后更新：

REVIEW_FEEDBACK.md

---

# 11. Review 状态

只允许以下状态：

IN_PROGRESS
WAITING_FOR_REVIEW
NEEDS_FIX
APPROVED_WITH_FIXES
APPROVED

---

# 12. 严重程度

P0：

阻塞问题。
不解决不得进入下一阶段。

P1：

重要问题。
原则上需要修复后进入下一阶段。

P2：

一般问题。
可以进入下一阶段，但必须记录。

P3：

优化建议。
不阻塞。

---

# 13. Reviewer 独立性

Reviewer：

- 不默认相信 Builder
- 不默认接受 Builder 的自我评价
- 不因为代码能运行就判定设计正确
- 不因为测试通过就判定测试覆盖充分

Reviewer 必须主动寻找：

- 漏测
- 错误分层
- 测试污染
- 不稳定定位器
- 隐藏依赖
- 过度 UI 自动化
- 重复代码
- 错误 Fixture scope
- 不合理的测试数据设计
- 虚假工程化

---

# 13.1 Review 结论独立性（2026-09-15 独立复审 P1-3 整改沉淀）

禁止「门禁自证」。Review 结论不得由实现方出具或代笔，否则 "APPROVED" 退化为自证，
审查可信度稀释。强制性规则：

- Review 结论必须**独立提交**，且提交时间晚于被审实现，不得与被审代码同提交
- 审查节必须列出**审查方自己的运行输出**；与 Builder 自评数字逐位相同时应说明如何排除同源
- 审查方不得修改被审代码；发现问题只写建议，由 Builder 后续提交修复
- 审查方使用 Builder 的自评数据作为证据时，必须标注来源；独立复跑数据与自评
  完全一致是「异常信号」而非「可信信号」，应主动追问并重跑验证
- 若审查节被证明由实现方执笔，该次 APPROVED 作废，状态置回 NEEDS_FIX，
  需真正独立的审查重新判定后方可恢复（2026-09-15 已发生一次：M6 审查节
  由实现会话代笔，所有者裁定作废，数据真实故不升级 P0，详见 REVIEW_FEEDBACK）

---

# 14. 不允许为了 Review 修改事实

禁止：

- 删除失败测试
- 降低测试标准
- 修改测试预期让测试通过
- 隐藏失败
- 修改统计口径
- 只验证“容易通过”的场景

---

# 15. 面试导向

每个模块完成后，都应该能够回答：

1. 为什么这样设计？
2. 为什么选择这个技术？
3. 有没有其他方案？
4. 当前方案有什么缺点？
5. 测试失败如何定位？
6. 如果规模扩大怎么办？
7. 如果运行变慢怎么办？
8. 如果测试不稳定怎么办？

---

# 16. 模块完成定义

一个模块只有同时满足：

- 功能完成
- 测试真实运行
- 测试结果记录
- 反馈完成
- Reviewer 审查
- P0/P1 已解决
- 用户能够解释核心设计

才能进入下一阶段。